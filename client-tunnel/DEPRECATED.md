# Substituído por client-agent/

Este diretório (`tgdesk-tunnel.exe`) foi consolidado em
[`../client-agent/`](../client-agent/) (`tgdesk-agent.exe technician`).
Mantido aqui só como histórico; não é mais usado pelos instaladores nem
lançado pelo Hub (ver `tunnel_helper.dart`).

# Substituído por client-agent/

Este diretório (`tgdesk-host.exe`) foi consolidado em
[`../client-agent/`](../client-agent/) (`tgdesk-agent.exe`, modo `host` —
padrão sem argumentos). Mantido aqui só como histórico; não é mais usado
pelos instaladores.
